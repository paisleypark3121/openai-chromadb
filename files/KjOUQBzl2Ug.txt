Cos'è un Access list un Access list è una serie di comandi che vengono configurati nel router e che consentono di controllare il traffico in ingresso in uscita sulla base di regole che appunto vengono inserite all'interno del router e che vengono applicate sui pacchetti in particolar modo analizzando leather dei pacchetti stessi e quindi possiamo stabilire di consentire del traffico o bloccare del traffico limitare alcuni pacchetti in arrivo da determinati sorgenti o verso determinati destinazioni e via dicendo Ma in che modo viene configurata quindi un assist non sono nient'altro che una serie di Access control entries quindi una serie di permit e the night statement che vengono inseriti all'interno del router stesso Quando il traffico quindi arriva l'interfaccia del router e ed è presente un Access list i router non fa nient'altro che comparare le informazioni che sono nel pacchetto rispetto ad ognuna delle entries che stanno all'interno della Access list del router in sequenziale e si verifica Quindi se una di queste viene soddisfatta o meno l'ultimo statement in un narcisista è sempre un implicit denai Quindi se non c'è nessun'altra riga precedentemente che viene soddisfatta In ogni caso ci sarà un implicit denai alla fine quindi il traffico verrà comunque bloccato Quindi se vogliamo consentire del traffico attenzione bisogna inserire un permette come ultimo statement se vogliamo bloccare quindi del traffico specifico ma consentire a tutto il resto quello che dobbiamo fare Inoltre è stabilire cosa è inbound E cos'è albound quindi un inbound rappresenta quindi il traffico Coming Into the router quindi Coming paquets Up Process before They a root to deadbound interface quindi prima che venga fatto routing del traffico verso l'interfaccia d'uscita viene fatto un processamento outbound invece viceversa quindi prima in Coming Pocket quindi coming out of the router Access list guidelines abbiamo due tipologie di Access list una standard dax list e un extended Access list Questo per avere all'interno del nostro sistema una maggiore efficienza e ridurre la ridondanza la x list standard hanno come caratteristica quella di avere un identificativo che va da 1 a 99 Mentre invece le extended da 100 a 199 e nell'axe stilista standard non si specifica il destination adrest per questo motivo devono essere configurate il più possibile vicino alla destinazione quello che si può fare con Access standard è il denaio il permit del Source ai pilates quindi si lavora solamente sull'ip sorgente ma non sulle IP destinazione per questo motivo devono essere configurate il più vicino possibile alla destinazione quindi nel router più vicino alla destinazione D'altra parte invece l'extendedex Fist invece viene si possono configurare quindi sia i denari per MIT per il Source piadress per destination pi address ma anche per le porte e può essere quindi configurata e deve essere configurata per una maggiore efficienza possibile più vicino possibile alla sorgente